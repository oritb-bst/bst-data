SELECT
    DOCPROJ   as PROJECT_ID,
	PROJDOCNO as PROJECT_NAME,
	SUPNAME   as CONTRACTOR_NAME,
	SUPDES    as CONTRACTOR_DES,
	BOOKNUM   as PARTIAL_INVOICE_NUM,
	CURDATE   as DATE,
	SOURCE_DB
FROM {{ source('bronze', 'MED_DOCUMENTS_P') }}