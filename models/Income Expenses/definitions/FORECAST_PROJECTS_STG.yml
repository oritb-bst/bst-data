  models:
  - name: FORECAST_PROJECTS_STG
    columns:
      - name: id
        data_tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn
         

